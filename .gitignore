# dependencies
node_modules/

# secrets — never commit (use js/keys.example.js as the template)
js/keys.js
.env
.env.*
worker/.env
!worker/.env.example

# brainstorm companion (local only)
.superpowers/

# os
.DS_Store
**/.DS_Store

# live snapshot (generated by the worker)
/snapshot.json

# playwright scratch
.playwright-mcp/

# verification screenshots
auspex-*.png
page-*.png
